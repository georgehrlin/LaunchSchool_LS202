The `color` property is inheritable. This means that applying `color` to an element also applies it to every descendant of that element. There are exceptions though, like links, whose colors are set separately by the browser.