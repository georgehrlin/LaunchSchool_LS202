Q: Can you see the background color you set on the `section` element? Why not? What can you add to your CSS to fix it?

A: I cannot see the background color of `section` at all. This is because its child elements—`article` and `aside`—are floated, so it no longer considers itself containing any child element and collapses its height. After `section`'s height has collapsed, the `article` and `aside` elements now hover above it and covers it completely.

To fix this, I can either add an `overflow` attribute to the `section`, or use clearfix on it.

LSBot Notes:
"This is because its child elements—`article` and `aside`—are floated, so it no longer considers itself containing any child element and collapses its height."
The `section` still contains them in the DOM, but floated elements are taken out of the normal flow, so their height doesn't contribute to the parent's height. That's why `section` collapses.