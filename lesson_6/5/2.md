Q: Can you determine why the blue paragraph hangs out beneath the right edge of the orange div?

A: I'm actually not sure. I don't know.

My understanding is, like how a floated image that sits in front (top-left) of a paragraph in a `.docx` document, we have a similar situation here. The floated `div` floats to the left of the `main` container, before the `p` can begin.

What I don't entirely understand is: because `div` is now floated, it should be excluded from the normal flow of the rendering of the page. Therefore, by default the `p` should begin below `div`, thus with the first parts of its first two lines covered.

Official Solution:
```css
main > p {
  clear: both;
}
```